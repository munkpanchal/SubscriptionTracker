// import { config } from "dotenv";

// config({
//     path: `.env.${
//         process.env.NODE_ENV || "development"
//     }.local`,
// });

// # Port
export const PORT = 5000;

// # ENVIRONMENT
export const NODE_ENV = "development";

// # Database
export const DB_URI =
    "mongodb+srv://mayankpanchal1801:mypass@cluster0.f2n2b.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0";

// export const { PORT, NODE_ENV, DB_URI } = process.env;
